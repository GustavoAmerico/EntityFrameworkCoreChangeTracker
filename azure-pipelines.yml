# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'windows-2024'


name: 10.0.$(Build.BuildId)
variables:
  version: 10.0.$(Build.BuildId)
 
stages:
  - stage: Continous_Integration
    displayName: Integração continua
    jobs:
      - job: Build
        variables:
          solution: src
        steps:
          - task: UseDotNet@2
            displayName: Instalando o .NET 10
            inputs:
              version: '10.x'
              packageType: sdk
              installationPath: $(Agent.ToolsDirectory)/dotnet

          - task: DotNetCoreCLI@2
            displayName: Compilando os pacotes a serem publicados
            inputs:
              projects: $(solution)
              arguments: '/p:Version=$(version) /p:PackageVersion=$(version)'

          - task: DotNetCoreCLI@2
            displayName: dotnet pack
            inputs:
              command: pack
              packagesToPack: $(solution)
              versioningScheme: 'Off'
              packDirectory: '$(Build.ArtifactStagingDirectory)/artifact'
              buildProperties: 'PackageVersion=$(version)'
              verbosityPack: Normal
              includesymbols: true
              includesource: true
              
            
          - task: PublishBuildArtifacts@1
            displayName: 'Publish Artifact'
            inputs:
              PathtoPublish: '$(Build.ArtifactStagingDirectory)'
              ArtifactName: artifact
              